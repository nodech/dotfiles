[user]
  email = nodar.chkuaselidze@gmail.com
  name = "Nodari Chkuaselidze"
  signingkey = 0x3A7B82E7241D78C4

[commit]
  gpgsign = true

[pull]
  ff = only

[core]
  excludesfile=/home/nd/.config/git/.gitignore_global
  hooksPath = /home/nd/.config/git/hooks
  pager = less -FRX
  pager = delta

[interactive]
  diffFilter = delta --color-only

[delta]
  navigate = true  # use n and N to move between diff sections
  dark = true      # or light = true, or omit for auto-detection
  line-numbers = true
  features = calochortus-lyallii-nod

[include]
  path = /home/nd/.config/git/delta-themes/themes.gitconfig

[merge]
    conflictStyle = zdiff3

[credential "https://github.com"]
  helper = 
  helper = !/usr/bin/gh auth git-credential

[init]
  defaultBranch = main

[alias]
  dlog = "!f() { GIT_EXTERNAL_DIFF='difft --display=inline' git log -p --ext-diff $@; }; f"
